# Host path to the NFS folder containing JSON alert files
INPUT_DIR_HOST=/path/to/your/nfs/alerts

# Optional: base URL to turn cutout filenames into URLs (leave empty to keep filenames/paths)
CUTOUT_BASE_URL=

# Live vs backfill
PUBLISH_MODE=live

# Only used in live mode. Leave empty to disable event-time cutoff.
CUTOFF_DAYS=3

# Polling interval (seconds)
POLL_SECONDS=2

# Kafka settings
TOPIC=last.alerts.raw
BOOTSTRAP_SERVERS=kafka:9092

# Kafka retention (milliseconds) - default 7 days
KAFKA_RETENTION_MS=604800000
